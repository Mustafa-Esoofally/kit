# Required
OPENAI_API_KEY=

# Required for Telegram interface (primary)
TELEGRAM_TOKEN=
# Optional — webhook secret token (recommended for production)
TELEGRAM_WEBHOOK_SECRET_TOKEN=

# Optional — Slack interface
SLACK_TOKEN=
SLACK_SIGNING_SECRET=

# Database (defaults suit the compose.yaml pgvector service)
DB_DRIVER=postgresql+psycopg
DB_HOST=localhost
DB_PORT=5432
DB_USER=ai
DB_PASS=ai
DB_DATABASE=ai

# Override the default model
KIT_MODEL=gpt-4o-mini

# User-authored context directory
KIT_CONTEXT_DIR=./context

# Runtime environment (prd enables JWT auth)
RUNTIME_ENV=dev
# Set APP_ENV=development to bypass Telegram webhook secret validation locally
APP_ENV=development
